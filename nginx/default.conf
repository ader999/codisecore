server {
    listen 80;
    server_name _;

    # Maximum allowed payload size for request body (supports up to 10+ high-res images per post)
    client_max_body_size 50M;
    client_body_buffer_size 10M;

    # Timeouts for slow mobile connections uploading large files
    client_body_timeout 300s;
    client_header_timeout 300s;
    keepalive_timeout 300s;
    send_timeout 300s;

    # Static files location
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    # Media files location
    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }

    # Reverse proxy to Django / Gunicorn
    location / {
        proxy_pass http://web:8000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for reading and sending data through reverse proxy
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;

        # Disable proxy request buffering for streaming large uploads if necessary
        # proxy_request_buffering off;
    }
}
